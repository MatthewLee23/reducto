1. you can use generate_neat_pdf.py to convert txt files to PDFs, and you can specify where the PDFs should be stored. 
*800-for-testing contains 800+ txt files of N-CSR filings for testing the system.
2. Run main.py on a new folder of PDFs and then the outputs including JSON extractions, splitting results, and validations with arithmetic errors will be stored in a new batch folder inside the outputs folder. 
